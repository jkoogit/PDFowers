# AGENTS.md

## 적용 범위

이 문서는 PDFowers 저장소 전체에 적용되는 작업 지침이다. 하위 폴더에 별도 `AGENTS.md`가 있으면 더 가까운 파일의 지침을 우선한다.

## 기본 언어

- 이 저장소의 이슈, PR, 리뷰, 문서, Codex 채팅 스레드 제목은 기본적으로 한글로 작성한다.
- 기술 용어, 명령어, 코드 식별자, 라이브러리명은 필요한 경우 영문 원문을 유지한다.
- 사용자가 별도로 요청하지 않아도 이 프로젝트에서는 한글 중심으로 작성한다.

## 브랜치 규칙

- 에이전트 새 스레드는 main 브랜치에서 시작한다.
- 작업 브랜치명은 `task/한글-작업명_에이전트명` 형식을 사용한다.
- 예: `task/문서정리-및-하네스준비_codex`
- 작업명은 가능한 한 짧고 명확하게 작성한다.
- 에이전트명은 작업 주체를 식별할 수 있게 작성한다. Codex 작업은 기본적으로 `codex`를 사용한다.
- 스레드 작업 내용을 PR할 때는 대상을 dev 브랜치로 한다.

## 이슈 규칙

- 이슈 제목은 한글로 작성한다.
- 이슈 본문은 한글로 작성한다.
- 이슈 본문에는 배경, 목표, 범위, 완료 조건, 관련 문서를 포함한다.
- 민감정보, 계정 비밀번호, DB 비밀번호, OAuth Client Secret, 계좌 정보, 개인정보 원문은 이슈에 기록하지 않는다.

## PR 규칙

- PR 제목은 한글로 작성한다.
- 이슈가 있는 작업의 PR 제목에는 추적 가능한 이슈 번호를 포함한다. 예: `세션 마무리 추적성 보완 (#63)`.
- PR 본문은 한글로 작성한다.
- PR 본문에는 작업 요약, 변경 파일, 검증 내용, 후속 작업을 포함한다.
- PR은 가능한 한 이슈 번호를 참조하고, 완료 대상 이슈가 있으면 `Closes #번호` 같은 GitHub 자동 연결 문구를 사용한다.
- PR 리뷰 제목과 리뷰 내용은 한글로 작성한다.

## 커밋 규칙

- 커밋 메시지는 한글 또는 한글 중심으로 작성한다.
- 커밋 메시지는 변경 의도를 한 줄로 명확하게 설명한다.
- 이슈가 있는 작업은 커밋 메시지나 PR 본문에서 이슈 번호를 참조한다.

## Codex 작업 규칙

- Codex 채팅 스레드 제목은 한글로 작성한다.
- Codex 검토 제목과 검토 내용은 한글로 작성한다.
- Codex가 생성하는 문서, 리뷰, 이슈, PR 본문은 사용자가 별도로 요청하지 않아도 한글로 작성한다.

## 브랜치 승급 규칙

- 기본 흐름은 `task/*` 작업 브랜치에서 PR을 생성하고, 검토 후 `dev`에 반영한다.
- `stg`, `main` 브랜치는 운영 정책이 확정된 뒤 `dev` 기준으로 승급처리한다.
- 배포 브랜치 승급은 관련 이슈와 PR 기록을 남긴다.
- 승급 결과의 최종 PR 번호, 커밋 SHA, 검증 결과는 관련 이슈 댓글에 기록한다.
- 승급 완료 후 결과 기록만을 위한 추가 커밋과 재승급은 원칙적으로 수행하지 않는다.
- 에이전트간 작업 충돌이 발생한 경우 그때그때 머지하고, 테스트를 거쳐 승급처리를 진행한다.

## 문서 관리 규칙

- 신규 프로젝트 기준 문서는 `docs/` 아래에 작성한다.
- 기존 참고 문서는 신규 프로젝트 문서로 승계하지 않고, 필요한 내용만 `docs/`에 재정리한다.
- 논리모순과 검토 필요사항은 `docs/08_이슈관리/논리모순_검토대장.md`에 기록한다.
- 리뷰 산출물은 `docs/10_리뷰관리/`에 기록한다.
