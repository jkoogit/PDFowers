# AGENTS.md

## 적용 범위

이 문서는 PDFowers 저장소 전체에 적용되는 AI 에이전트(Codex, Gemini 등) 공통 작업 지침이다. 하위 폴더에 별도 `AGENTS.md`가 있으면 더 가까운 파일의 지침을 우선한다.

## 기본 언어

- 이 저장소의 이슈, PR, 리뷰, 문서, 에이전트 채팅 스레드 제목 및 대화는 기본적으로 한글로 작성한다.
- 기술 용어, 명령어, 코드 식별자, 라이브러리명은 필요한 경우 영문 원문을 유지한다.
- 사용자가 별도로 요청하지 않아도 이 프로젝트에서는 한글 중심으로 작성한다.

## 브랜치 규칙

- 에이전트 새 스레드는 main 브랜치에서 시작한다.
- 작업 브랜치명은 `task/한글-작업명_에이전트명` 형식을 사용한다.
- 예: `task/하네스규칙현행화-및-백로그체계구축_gemini`, `task/문서정리-및-하네스준비_codex`
- 작업명은 가능한 한 짧고 명확하게 작성한다.
- 에이전트명은 작업 주체를 식별할 수 있게 작성한다 (`gemini`, `codex` 등).
- 스레드 작업 내용을 PR할 때는 대상을 dev 브랜치로 한다.

## 하네스 작업 단계 및 파일 수정 권한 (⭐️ 핵심 규칙)

AI 에이전트의 모든 작업은 하네스의 **세션(Session) - 태스크(Task) - 루프(Loop)** 3계층 단계에 따라 수행해야 하며, 파일 수정 권한을 엄격히 준수한다.

> [!IMPORTANT]
> **문서, 코드, 소스 수정 작업은 오직 `태스크처리`, `세션정리`, `루프처리` 단계에서만 가능하다.**<br>
> 그 외의 모든 단계는 Read-Only / 계획 수립 / 검증 / 상태 확인만 수행하며, 파일 수정을 엄격히 금지한다.

| 계층 | 단계 (트리거) | 파일 수정 허용 여부 | 주요 실행 업무 |
| :--- | :--- | :---: | :--- |
| **세션** | **`#세션시작`** | ❌ **수정 불가 (Read-Only)** | 이전 세션 인계사항 확인, git 브랜치/이슈 점검, 세션 목표 수립 |
| | **`#세션정리`** | 🟢 **수정 가능 (Write 허용)** | 세션명 확정, 세션 리뷰 및 **회고 문서** 작성, **백로그 대장** 갱신, `dev` 대상 PR 생성, 관련 문서 현행화 |
| | **`#세션종료`** | ❌ **수정 불가 (Read-Only)** | 채팅으로 다음 세션명/남은 현황/인계사항 안내 (커밋/문서작성 없음) |
| **태스크** | **`#태스크시작`** | ❌ **수정 불가 (Read-Only)** | 작업 브랜치(`task/한글-작업명_에이전트명`) 생성/확인, 요구사항 분석 및 구현 계획 수립 |
| | **`#태스크처리`** | 🟢 **수정 가능 (Write 허용)** | **실제 소스 코드, 테스트 코드, 설정, 관련 문서 작성 및 수정** |
| | **`#태스크정리`** | ❌ **수정 불가 (Read-Only)** | 빌드/타입체크/테스트 검증 실행(`npm test` 등), 변경사항 요약 및 커밋 생성 |
| | **`#태스크승급`** | ❌ **수정 불가 (Read-Only)** | `dev -> stg -> main` 직접 머지, 브랜치 commit SHA 동기화 점검, 이슈 상태 갱신/종료 |
| **루프** | **`#루프분석`** | ❌ **수정 불가 (Read-Only)** | 반복 단위 작업(TDD, 리팩터링 등)의 WorkItem, 의존성, 완료조건, 허용경로, 검증명령 정의 |
| | **`#루프실행` / `#루프처리`** | 🟢 **수정 가능 (Write 허용)** | checkpoint 생성 후 **해당 WorkItem에 국한된 소스 수정, 테스트 작성 및 리팩터링 실행** |
| | **`#루프보완`** | ❌ **수정 불가 (Read-Only)** | 검증 실패 또는 새로운 발견 항목 발생 시 WorkItem 정의 보완 |
| | **`#루프승인`** | ❌ **수정 불가 (Read-Only)** | 사용자 승인 필요 항목, 예외 처리 승인 및 루프 종료 판정 |

## 단계 전환 안내 규칙 (복사용 코드블록)

- 에이전트가 특정 단계를 완료하고 다음 단계를 제안할 때는, 사용자가 복사하여 즉시 입력할 수 있도록 **다음 명령 코드블록(`text`)**을 응답 마지막에 항상 제시한다.

## 이슈 규칙

- 이슈 제목은 한글로 작성한다.
- 세션 마무리에서 신규 이슈를 생성하면 생성 후 발급된 이슈 번호를 제목 앞에 표시하도록 제목을 갱신한다. 예: `#74 세션 워크플로 트리거 정리`.
- 이슈 본문은 한글로 작성한다.
- 이슈 본문에는 배경, 목표, 범위, 완료 조건, 관련 문서를 포함한다.
- 민감정보, 계정 비밀번호, DB 비밀번호, OAuth Client Secret, 계좌 정보, 개인정보 원문은 이슈에 기록하지 않는다.

## PR 규칙

- PR 제목은 한글로 작성한다.
- 이슈가 있는 작업의 PR 제목에는 추적 가능한 이슈 번호를 포함한다. 예: `세션 마무리 추적성 보완 (#63)`.
- PR 본문은 한글로 작성한다.
- PR 본문에는 작업 요약, 변경 파일, 검증 내용, 후속 작업을 포함한다.
- PR은 가능한 한 이슈 번호를 참조하고, 완료 대상 이슈가 있으면 `Closes #번호` 같은 GitHub 자동 연결 문구를 사용한다.
- PR 리뷰 제목과 리뷰 내용은 한글로 작성한다.

## 커밋 규칙

- 커밋 메시지는 한글 또는 한글 중심으로 작성한다.
- 커밋 메시지는 변경 의도를 한 줄로 명확하게 설명한다.
- 이슈가 있는 작업은 커밋 메시지나 PR 본문에서 이슈 번호를 참조한다.

## AI 에이전트 공통 작업 규칙

- 에이전트 채팅 스레드 제목은 한글로 작성한다.
- 에이전트 검토 제목과 검토 내용은 한글로 작성한다.
- 에이전트가 생성하는 문서, 리뷰, 이슈, PR 본문은 사용자가 별도로 요청하지 않아도 한글로 작성한다.

## 브랜치 승급 규칙

- 기본 흐름은 `task/*` 작업 브랜치에서 PR을 생성하고, 검토 후 `dev`에 반영한다.
- `stg`, `main` 브랜치는 `dev` 기준으로 승급처리한다. `main`은 GitHub 기본 운영 브랜치이며 서비스 관점의 prd에 해당한다.
- 배포 브랜치 승급은 별도 승급 PR을 만들지 않고 직접 머지한다.
- 승급 머지 메시지에는 관련 이슈 번호와 원천 작업 PR 번호를 표시한다.
- 승급 결과의 원천 PR 번호, 승급 머지 커밋 SHA, 검증 결과는 관련 이슈 댓글에 기록한다.
- 승급 완료 후 결과 기록만을 위한 추가 커밋과 재승급은 원칙적으로 수행하지 않는다.
- 에이전트간 작업 충돌이 발생한 경우 그때그때 머지하고, 테스트를 거쳐 승급처리를 진행한다.

## 문서 및 로그 관리 규칙

- 신규 프로젝트 기준 문서는 `docs/` 아래에 작성한다.
- 기존 참고 문서는 신규 프로젝트 문서로 승계하지 않고, 필요한 내용만 `docs/`에 재정리한다.
- 논리모순과 검토 필요사항은 `docs/08_이슈관리/논리모순_검토대장.md`에 기록한다.
- 리뷰 및 회고 산출물은 `docs/10_리뷰관리/`에 기록한다.
- 작업 중 발생하는 부가 작업, 기술 부채, 후속 아이디어, 발견 항목은 `docs/11_로그/백로그_대장.md`에 기록하여 관리한다.
