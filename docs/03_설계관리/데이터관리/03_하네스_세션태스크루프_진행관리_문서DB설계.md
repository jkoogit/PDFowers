# 03. 하네스 세션·태스크·루프 진행관리 문서 DB 설계서

## 1. 개요 및 설계 원칙

본 문서는 PDFowers 프로젝트의 AI 에이전트 하네스 3계층(**세션 - 태스크 - 루프**)의 진행 정보와 작업 그래프, 백로그, 회고, 마크다운 문서 메타데이터를 체계적이고 유연하게 관리하기 위한 **문서 DB(Document Store / JSON Document) 및 영속화 스키마**를 정의한다.

### 1.1 설계 원칙
1. **문서 우선 및 단일 진실 공급원 (Markdown & Code First SSOT)**:
   - 물리적 마크다운 문서(`/docs/*.md`)와 Git 소스 코드가 절대적인 원천(Source of Truth)이다.
   - DB는 검색 가속, 대시보드 뷰어 렌더링, 작업 진행도 추적, API 연계를 위한 **"읽기 최적화 프로젝션(Read-Model)"**으로 동작한다.
2. **문서 DB(Document Store)의 유연성 확보**:
   - 세션/태스크/루프의 메트릭, AI 메타, 작업 그래프, diff 요약 등 진화가 빠른 비정형 데이터는 **JSON Document Schema**로 유연하게 수용한다.
   - 검색 빈도가 높고 관계 무결성이 필요한 핵심 키(ID, 상태, 일시, 버전)는 관계형 인덱스/메타 테이블로 함께 관리한다.
3. **낙관적 잠금 (Optimistic Locking)**:
   - 모든 엔티티에 `version` 정수 필드를 두어 동시 수정 충돌 및 경합을 방지한다.
4. **상시 자가 치유 (Reconciliation)**:
   - 상위 요약 정보와 하위 상세 정보 간의 상태 불일치가 감지되면 언제든 하위 문서를 기준으로 상위를 자동 보정하는 Reconciler 메커니즘을 지원한다.

---

## 2. 공통 메타데이터 (Audit Meta)

모든 엔티티와 JSON Document는 [`00_데이터관리설계기준.md`](file:///d:/dev/workspace/git.jkoogit/PDFowers/docs/03_설계관리/데이터관리/00_데이터관리설계기준.md)의 공통 7종 메타 규격을 준수한다.

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AuditMeta",
  "type": "object",
  "required": ["created_sys", "created_at", "created_by", "updated_sys", "updated_at", "updated_by", "version"],
  "properties": {
    "created_sys": { "type": "string", "description": "최초 등록 시스템 (예: harness-cli, agent-service)" },
    "created_at": { "type": "string", "format": "date-time", "description": "최초 등록 일시 (ISO-8601)" },
    "created_by": { "type": "string", "description": "최초 등록자 (에이전트명 또는 사용자ID, 예: gemini, codex, jkoogi)" },
    "updated_sys": { "type": "string", "description": "최종 수정 시스템" },
    "updated_at": { "type": "string", "format": "date-time", "description": "최종 수정 일시 (ISO-8601)" },
    "updated_by": { "type": "string", "description": "최종 수정자" },
    "version": { "type": "integer", "minimum": 1, "description": "낙관적 잠금 버전 번호" }
  }
}
```

---

## 3. 세션 문서 (Session Document) 스키마

### 3.1 JSON Schema 정의
- **컬렉션/테이블명**: `harness_session_doc`
- **주요 목적**: 세션 전체의 목표, 작업 그룹, 연관 세션, 실행 태스크 요약, 이슈/회고/백로그 연계 및 세션 단위 작업 그래프 관리

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionDocument",
  "type": "object",
  "required": ["session_id", "session_name", "work_group", "status", "started_at", "ai_meta", "meta"],
  "properties": {
    "session_id": { "type": "string", "description": "세션 고유 식별자 (예: SES-20260915-01 또는 16)" },
    "session_name": { "type": "string", "description": "세션 명칭 (예: 16-세션_태스크_진행관리_문서DB_기획분석설계)" },
    "parent_session_id": { "type": ["string", "null"], "description": "상위 세션 ID" },
    "related_sessions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["session_id", "relation_type"],
        "properties": {
          "session_id": { "type": "string" },
          "relation_type": { "type": "string", "enum": ["PRECEDING", "FOLLOWING", "PARENT", "CHILD", "REFERENCE"] }
        }
      }
    },
    "work_group": {
      "type": "array",
      "items": { "type": "string", "enum": ["기획", "분석", "설계", "개발", "테스트", "리뷰", "운영"] },
      "description": "세션 작업 성격 분류"
    },
    "status": {
      "type": "string",
      "enum": ["시작", "작업", "정리", "종료", "보류", "폐기"],
      "description": "세션 진행 상태"
    },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": ["string", "null"], "format": "date-time" },
    "ai_meta": {
      "type": "object",
      "required": ["agent", "model", "version", "agent_status"],
      "properties": {
        "agent": { "type": "string", "description": "에이전트명 (예: gemini, codex)" },
        "model": { "type": "string", "description": "모델명 (예: gemini-3.7-flash, gpt-4o)" },
        "version": { "type": "string", "description": "하네스/에이전트 버전" },
        "agent_status": { "type": "string", "description": "에이전트 상태 (예: IDLE, ACTIVE, HANDOFF)" },
        "total_token_usage": {
          "type": "object",
          "properties": {
            "prompt_tokens": { "type": "integer" },
            "completion_tokens": { "type": "integer" },
            "total_tokens": { "type": "integer" }
          }
        }
      }
    },
    "git_issue": {
      "type": ["object", "null"],
      "properties": {
        "issue_number": { "type": "integer" },
        "title": { "type": "string" },
        "body": { "type": "string" },
        "url": { "type": "string", "format": "uri" }
      }
    },
    "retrospective": {
      "type": ["object", "null"],
      "properties": {
        "doc_id": { "type": "string" },
        "doc_path": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" },
        "author": { "type": "string" },
        "summary": { "type": "string" }
      }
    },
    "backlog_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "이 세션에서 도출되거나 해결된 공식 백로그 ID 목록"
    },
    "task_list": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["task_id", "task_name", "status"],
        "properties": {
          "task_id": { "type": "string" },
          "task_name": { "type": "string" },
          "status": { "type": "string" },
          "started_at": { "type": "string", "format": "date-time" },
          "ended_at": { "type": ["string", "null"], "format": "date-time" }
        }
      },
      "description": "세션에 포함된 하위 태스크 요약 캐시 (순서 및 진행률)"
    },
    "task_graph": {
      "type": "object",
      "description": "세션 전체의 작업 그래프 (DAG)",
      "properties": {
        "nodes": { "type": "array", "items": { "type": "object" } },
        "edges": { "type": "array", "items": { "type": "object" } },
        "current_node_id": { "type": ["string", "null"] }
      }
    },
    "meta": { "$ref": "#/definitions/AuditMeta" }
  }
}
```

---

## 4. 태스크 문서 (Task Document) 스키마

### 4.1 JSON Schema 정의
- **컬렉션/테이블명**: `harness_task_doc`
- **주요 목적**: 단위 태스크의 생명주기, Git 브랜치/PR, 포함된 루프 목록, 임시 백로그 수집 및 태스크 회고 관리

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TaskDocument",
  "type": "object",
  "required": ["task_id", "session_id", "task_name", "status", "started_at", "git_info", "meta"],
  "properties": {
    "task_id": { "type": "string", "description": "태스크 고유 식별자 (예: TSK-20260915-01)" },
    "session_id": { "type": "string", "description": "소속 세션 ID (역참조 FK)" },
    "task_name": { "type": "string", "description": "태스크 명칭 (예: 세션-태스크-루프_진행관리_설계문서화)" },
    "status": {
      "type": "string",
      "enum": ["시작", "분석", "처리", "정리", "승급", "완료", "보류", "폐기"],
      "description": "태스크 진행 상태"
    },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": ["string", "null"], "format": "date-time" },
    "git_info": {
      "type": "object",
      "required": ["branch"],
      "properties": {
        "branch": { "type": "string", "description": "작업 브랜치명 (예: task/문서정리_gemini)" },
        "start_commit": { "type": ["string", "null"] },
        "end_commit": { "type": ["string", "null"] },
        "pr": {
          "type": ["object", "null"],
          "properties": {
            "pr_number": { "type": "integer" },
            "title": { "type": "string" },
            "body": { "type": "string" },
            "url": { "type": "string", "format": "uri" },
            "merged_at": { "type": ["string", "null"], "format": "date-time" }
          }
        },
        "promotion_commits": {
          "type": "object",
          "properties": {
            "dev_to_stg_sha": { "type": ["string", "null"] },
            "stg_to_main_sha": { "type": ["string", "null"] }
          }
        }
      }
    },
    "draft_backlogs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["draft_id", "title", "content", "status"],
        "properties": {
          "draft_id": { "type": "string", "description": "임시 식별자 (예: DRAFT-01)" },
          "title": { "type": "string" },
          "content": { "type": "string" },
          "status": { "type": "string", "enum": ["DRAFT", "PROMOTED", "TRANSFERRED_TO_OFFICIAL", "DISCARDED"] },
          "promoted_task_id": { "type": ["string", "null"] },
          "official_backlog_id": { "type": ["string", "null"] }
        }
      },
      "description": "태스크 도중 발굴된 임시 아이디어/기술부채 목록"
    },
    "loop_list": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["loop_id", "loop_name", "status"],
        "properties": {
          "loop_id": { "type": "string" },
          "loop_name": { "type": "string" },
          "status": { "type": "string" },
          "started_at": { "type": "string", "format": "date-time" },
          "ended_at": { "type": ["string", "null"], "format": "date-time" }
        }
      },
      "description": "태스크에 포함된 루프 요약 캐시"
    },
    "retrospective": {
      "type": ["object", "null"],
      "properties": {
        "summary": { "type": "string" },
        "completed_items": { "type": "array", "items": { "type": "string" } },
        "verification_result": { "type": "string" }
      }
    },
    "task_graph": {
      "type": "object",
      "description": "태스크 내부의 루프/스텝별 세부 작업 그래프"
    },
    "meta": { "$ref": "#/definitions/AuditMeta" }
  }
}
```

---

## 5. 루프 문서 (Loop Document) 스키마

### 5.1 JSON Schema 정의
- **컬렉션/테이블명**: `harness_loop_doc`
- **주요 목적**: TDD, 단위 리팩터링 등 초단위/분단위 반복 작업의 WorkItem 명세, 체크포인트, 코드 diff, 검증 결과 및 제어(정지/보류/보완) 상태 관리

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LoopDocument",
  "type": "object",
  "required": ["loop_id", "task_id", "session_id", "loop_name", "status", "started_at", "work_item", "meta"],
  "properties": {
    "loop_id": { "type": "string", "description": "루프 고유 식별자 (예: LP-20260915-01)" },
    "task_id": { "type": "string", "description": "직속 부모 태스크 ID (역참조 FK)" },
    "session_id": { "type": "string", "description": "소속 세션 ID" },
    "loop_name": { "type": "string", "description": "루프 작업명" },
    "status": {
      "type": "string",
      "enum": ["분석", "기획", "설계", "처리", "정리", "정지", "보류", "보완", "폐기"],
      "description": "루프 진행 상태"
    },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": ["string", "null"], "format": "date-time" },
    "work_item": {
      "type": "object",
      "required": ["objective", "scope", "allow_paths", "completion_criteria", "verify_command"],
      "properties": {
        "objective": { "type": "string", "description": "루프 목표" },
        "scope": { "type": "string", "description": "작업 범위" },
        "allow_paths": {
          "type": "array",
          "items": { "type": "string" },
          "description": "수정 허용 파일/디렉토리 경로 (권한 제어)"
        },
        "completion_criteria": {
          "type": "array",
          "items": { "type": "string" },
          "description": "완료 조건 체크리스트"
        },
        "verify_command": { "type": "string", "description": "단위 검증 실행 명령어 (예: npm test -- test/unit/...)" }
      }
    },
    "checkpoint": {
      "type": ["object", "null"],
      "properties": {
        "commit_hash": { "type": "string", "description": "롤백 기준 git commit sha" },
        "snapshot_ref": { "type": "string", "description": "임시 스냅샷 참조 ID" }
      }
    },
    "diff_summary": {
      "type": ["object", "null"],
      "properties": {
        "files_changed": { "type": "integer" },
        "insertions": { "type": "integer" },
        "deletions": { "type": "integer" },
        "diff_text": { "type": "string", "description": "주요 변경 diff 요약" }
      }
    },
    "verify_result": {
      "type": ["object", "null"],
      "properties": {
        "status": { "type": "string", "enum": ["SUCCESS", "FAILED", "SKIPPED"] },
        "executed_command": { "type": "string" },
        "logs": { "type": "string" },
        "passed_at": { "type": "string", "format": "date-time" }
      }
    },
    "control_log": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "action": { "type": "string", "enum": ["PAUSE", "HOLD", "REVISE", "RESUME", "ABORT"] },
          "reason": { "type": "string" },
          "timestamp": { "type": "string", "format": "date-time" }
        }
      },
      "description": "정지/보류/보완/재개 이력"
    },
    "meta": { "$ref": "#/definitions/AuditMeta" }
  }
}
```

---

## 6. 작업 그래프 (Work Graph / DAG) 데이터 모델

세션, 태스크, 백로그 간의 인과관계 및 선후 의존성을 비순환 방향 그래프(DAG)로 관리한다.

```json
{
  "graph_id": "GRAPH-SES-20260915-01",
  "session_id": "SES-20260915-01",
  "nodes": [
    {
      "id": "TSK-01",
      "type": "TASK",
      "label": "세션태스크루프_진행관리_설계문서화",
      "status": "PROCESSING",
      "dependencies": []
    },
    {
      "id": "DRAFT-01",
      "type": "DRAFT_BACKLOG",
      "label": "에이전트 서비스 REST API 구현",
      "status": "PENDING",
      "dependencies": ["TSK-01"]
    }
  ],
  "edges": [
    {
      "source": "TSK-01",
      "target": "DRAFT-01",
      "relation": "TRIGGERS"
    }
  ]
}
```

---

## 7. 마크다운 문서 메타 및 프로젝션 스키마 (`docs_meta`)

- **컬렉션/테이블명**: `agent_docs_meta`
- **역할**: `/docs` 하위 마크다운 파일들의 메타정보, 해시값, 파싱된 헤딩 트리, 연관 링크 그래프를 보관하여 뷰어 및 검색 가속 지원

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AgentDocsMeta",
  "type": "object",
  "required": ["doc_id", "file_path", "category", "title", "content_hash", "last_synced_at", "meta"],
  "properties": {
    "doc_id": { "type": "string", "description": "문서 고유 ID (예: DOC-DESIGN-03)" },
    "file_path": { "type": "string", "description": "상대 파일 경로 (예: docs/03_설계관리/데이터관리/03_설계서.md)" },
    "category": { "type": "string", "enum": ["산출물", "설계관리", "기술관리", "이슈관리", "리뷰관리", "로그", "루트"] },
    "title": { "type": "string", "description": "문서 제목 (H1)" },
    "content_hash": { "type": "string", "description": "SHA-256 파일 내용 해시 (정합성 검증용)" },
    "frontmatter": { "type": "object", "description": "YAML Frontmatter 속성 (작성자, 태그, 상태 등)" },
    "heading_tree": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "level": { "type": "integer" },
          "text": { "type": "string" },
          "anchor": { "type": "string" }
        }
      }
    },
    "outbound_links": {
      "type": "array",
      "items": { "type": "string" },
      "description": "본문에서 참조하는 다른 문서 경로 목록"
    },
    "last_synced_at": { "type": "string", "format": "date-time" },
    "meta": { "$ref": "#/definitions/AuditMeta" }
  }
}
```

---

## 8. 관계형 인덱스 테이블 DDL (PostgreSQL / Drizzle 기준)

문서 DB의 빠른 검색 및 상위 무결성 보장을 위해 메타/인덱스 RDB 테이블을 병행 구성한다.

```sql
-- 1. 세션 인덱스 테이블
CREATE TABLE harness_session_meta (
    session_id VARCHAR(64) PRIMARY KEY,
    session_name VARCHAR(255) NOT NULL,
    work_group VARCHAR(100) NOT NULL,
    status_cd VARCHAR(30) NOT NULL,
    ai_agent VARCHAR(50) NOT NULL,
    ai_model VARCHAR(50) NOT NULL,
    started_at TIMESTAMPTZ NOT NULL,
    ended_at TIMESTAMPTZ,
    doc_payload JSONB NOT NULL, -- SessionDocument 전체 본문
    created_sys VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by VARCHAR(50) NOT NULL,
    updated_sys VARCHAR(50) NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by VARCHAR(50) NOT NULL,
    version INTEGER NOT NULL DEFAULT 1
);

-- 2. 태스크 인덱스 테이블
CREATE TABLE harness_task_meta (
    task_id VARCHAR(64) PRIMARY KEY,
    session_id VARCHAR(64) NOT NULL REFERENCES harness_session_meta(session_id) ON DELETE CASCADE,
    task_name VARCHAR(255) NOT NULL,
    status_cd VARCHAR(30) NOT NULL,
    git_branch VARCHAR(255) NOT NULL,
    started_at TIMESTAMPTZ NOT NULL,
    ended_at TIMESTAMPTZ,
    doc_payload JSONB NOT NULL, -- TaskDocument 전체 본문
    created_sys VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by VARCHAR(50) NOT NULL,
    updated_sys VARCHAR(50) NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by VARCHAR(50) NOT NULL,
    version INTEGER NOT NULL DEFAULT 1
);

-- 3. 루프 인덱스 테이블
CREATE TABLE harness_loop_meta (
    loop_id VARCHAR(64) PRIMARY KEY,
    task_id VARCHAR(64) NOT NULL REFERENCES harness_task_meta(task_id) ON DELETE CASCADE,
    session_id VARCHAR(64) NOT NULL,
    loop_name VARCHAR(255) NOT NULL,
    status_cd VARCHAR(30) NOT NULL,
    started_at TIMESTAMPTZ NOT NULL,
    ended_at TIMESTAMPTZ,
    doc_payload JSONB NOT NULL, -- LoopDocument 전체 본문
    created_sys VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by VARCHAR(50) NOT NULL,
    updated_sys VARCHAR(50) NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by VARCHAR(50) NOT NULL,
    version INTEGER NOT NULL DEFAULT 1
);

-- 4. 문서 프로젝션 메타 테이블
CREATE TABLE agent_docs_meta (
    doc_id VARCHAR(64) PRIMARY KEY,
    file_path VARCHAR(500) UNIQUE NOT NULL,
    category VARCHAR(50) NOT NULL,
    title VARCHAR(255) NOT NULL,
    content_hash VARCHAR(64) NOT NULL,
    last_synced_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    doc_payload JSONB NOT NULL, -- AgentDocsMeta 전체 본문
    created_sys VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by VARCHAR(50) NOT NULL,
    updated_sys VARCHAR(50) NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by VARCHAR(50) NOT NULL,
    version INTEGER NOT NULL DEFAULT 1
);

-- 인덱스 생성
CREATE INDEX idx_harness_session_status ON harness_session_meta(status_cd);
CREATE INDEX idx_harness_task_session_status ON harness_task_meta(session_id, status_cd);
CREATE INDEX idx_harness_loop_task_status ON harness_loop_meta(task_id, status_cd);
CREATE INDEX idx_agent_docs_category ON agent_docs_meta(category);
```

---

## 9. 자가 치유(Reconciler) 및 정합성 검증 흐름

1. **상시 Reconcile 실행 (`reconcileSession(sessionId)`)**:
   - `harness_task_meta`에서 해당 세션의 모든 태스크를 조회.
   - 세션 문서의 `task_list` 배열과 실제 태스크 목록 및 상태를 비교.
   - 불일치가 있을 경우 하위 태스크 실제 레코드를 기준으로 상위 세션 문서의 `task_list`를 덮어쓰고 `version = version + 1` 갱신.
2. **루프 가드 (Loop Guard 검증)**:
   - 태스크 종료/정리 시점에 해당 태스크에 속한 모든 루프의 `status_cd`가 `정리(COMPLETED)` 또는 `정지(ABORTED)`인지 검사.
   - 미완료 루프가 존재할 경우 태스크 정리 거부(Exception).
3. **문서 동기화 및 크로스체크 (`verifyDocsConsistency()`)**:
   - 로컬 `/docs` 디렉토리의 모든 `.md` 파일의 SHA-256 해시를 계산.
   - DB `agent_docs_meta`의 `content_hash`와 대조하여 누락 또는 변경된 파일을 즉시 파싱 및 동기화.

---

## 10. 관련 문서

- [00. 데이터관리 설계기준](./00_데이터관리설계기준.md)
- [13. 에이전트 서비스 API 명세서](../../04_기술관리/13_에이전트서비스_API_명세.md)
- [09. 하네스 작업 운영 절차](../../00_산출물/02_하네스산출물/09_하네스작업운영절차.md)
- [AGENTS.md](../../../AGENTS.md)

---

## 작업 이력

| 작업일시 | 작업 에이전트 | 작성자 | 내용 한 줄 요약 |
| :--- | :--- | :--- | :--- |
| 2026-09-15 01:00 KST | Gemini | jkoogi | 하네스 세션·태스크·루프 진행관리 문서DB 및 영속화 스키마 설계서 최초 작성 |
