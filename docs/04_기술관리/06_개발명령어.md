# 06. 개발 명령어

## 목적

하네스 기반 PDFowers 개발자가 로컬에서 반복 실행할 명령을 정리한다. 현재 저장소에는 앱 소스가 없으므로 즉시 실행 가능한 명령과 앱 스캐폴딩 이후 사용할 명령을 분리한다.

## 현재 즉시 실행 가능한 명령

| 목적 | 명령 | 기준 |
| :--- | :--- | :--- |
| Git 상태 확인 | `git status --short --branch` | 작업 브랜치와 변경 파일을 확인한다. |
| 문서 공백 검사 | `git diff --check` | Markdown 변경의 trailing whitespace를 확인한다. |
| 파일 목록 확인 | `rg --files` | 저장소 문서와 소스 생성 여부를 확인한다. |
| 의존성 설치 | `npm install` | `package-lock.json` 기준으로 의존성을 설치한다. |
| 전체 테스트 | `npm test` | Vitest로 단위/통합 테스트를 함께 실행한다. DB 환경변수가 없으면 통합 테스트는 건너뛴다. |
| 단위 테스트 | `npm run test:unit` | DB 없이 인증 도메인 순수 로직 테스트를 실행한다. |
| 통합 테스트 | `npm run test:integration` | `TEST_DATABASE_URL` 또는 `DATABASE_URL` 기준으로 PostgreSQL 통합 테스트를 실행한다. |
| 타입 검사 | `npm run typecheck` | TypeScript 타입 오류를 확인한다. |

## 앱 스캐폴딩 이후 추가 표준 명령

| 목적 | 명령 | 기준 |
| :--- | :--- | :--- |
| 개발 서버 | `npm run dev` | 로컬 API 또는 웹 서버를 실행한다. |
| 테스트 감시 | `npm run test:watch` | 개발 중 변경 파일 기준 테스트를 반복 실행한다. |
| 린트 | `npm run lint` | 코드 스타일과 정적 규칙을 확인한다. |
| 빌드 | `npm run build` | 배포 가능한 산출물을 생성한다. |

## DB와 ORM 명령 후보

Drizzle ORM 도입 후 다음 명령을 표준 후보로 둔다.

| 목적 | 명령 | 기준 |
| :--- | :--- | :--- |
| 마이그레이션 생성 | `npm run db:generate` | `src/db/schema`의 Drizzle 스키마에서 SQL 마이그레이션을 생성한다. |
| 마이그레이션 적용 | `npm run db:migrate` | 로컬 또는 테스트 DB에 마이그레이션을 적용한다. |
| DB 스튜디오 | `npm run db:studio` | 로컬 개발 중 스키마와 데이터를 확인한다. |

## 테스트 게이트

MVP1 인증 PR은 최소한 다음 명령을 통과해야 한다.

```text
npm test
npm run test:unit
npm run test:integration
npm run typecheck
```

린트 도구가 도입되면 `npm run lint`를 게이트에 추가한다.

## Node/npm PATH 기준

Windows 로컬 환경에서 `node`가 WindowsApps 아래 Codex 앱 내부 실행 파일을 먼저 잡으면 `Access is denied`가 발생할 수 있다. 이 경우 사용자 PATH에서 정식 Node 설치 경로가 `WindowsApps`보다 앞에 있어야 한다.

현재 확인된 사용자 설치 경로는 다음과 같다.

```text
C:\Users\jkoog\.local\nodejs\node-v26.3.0-win-x64
```

PowerShell에서 `npm.ps1` 실행 정책 오류가 발생하면 `npm.cmd`가 사용되도록 확인한다.

## 환경변수 기준

| 변수 | 목적 | 민감정보 여부 |
| :--- | :--- | :---: |
| `DATABASE_URL` | PostgreSQL 연결 | 예 |
| `TEST_DATABASE_URL` | 통합 테스트용 PostgreSQL 연결. 없으면 `DATABASE_URL`을 사용 | 예 |
| `SESSION_SECRET` | 서버 세션 서명 | 예 |
| `OAUTH_KAKAO_CLIENT_ID` | 카카오 OAuth 앱 ID | 아니오 |
| `OAUTH_KAKAO_CLIENT_SECRET` | 카카오 OAuth Secret | 예 |
| `OAUTH_NAVER_CLIENT_ID` | 네이버 OAuth 앱 ID | 아니오 |
| `OAUTH_NAVER_CLIENT_SECRET` | 네이버 OAuth Secret | 예 |
| `OAUTH_GOOGLE_CLIENT_ID` | 구글 OAuth 앱 ID | 아니오 |
| `OAUTH_GOOGLE_CLIENT_SECRET` | 구글 OAuth Secret | 예 |

민감정보 원문은 문서, 이슈, PR, 테스트 로그에 기록하지 않는다.

## 작업 이력

| 작업일시 | 작업 에이전트 | 작성자 | 내용 한 줄 요약 |
| :--- | :--- | :--- | :--- |
| 2026-06-10 00:00 KST | Codex | jkoogi | 하네스 개발 명령어와 MVP1 인증 검증 게이트 초안 작성 |
| 2026-06-10 00:00 KST | Codex | jkoogi | TypeScript/Vitest 도입 후 현재 표준 명령과 Node/npm PATH 기준 갱신 |
| 2026-06-10 22:00 KST | Codex | jkoogi | 단위/통합 테스트 명령과 DB 환경변수 기준 분리 |
