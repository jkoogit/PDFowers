# 20260612 MVP1 잔여작업 결정 및 진행 정리

## 목적

MVP1 인증/검수 작업의 남은 범위를 재정리하고, 이번 세션에서 확정된 결정과 후속 진행 순서를 기록한다.

## 확정 결정

| 항목 | 결정 | 후속 영향 |
| :--- | :--- | :--- |
| 이메일 인증 | dev/stg/prd별 차이를 두지 않고 설정값에 따라 서비스 적용한다. 구현 단계에서는 인증하도록 구현한다. | 이메일 인증 API와 화면 흐름을 MVP1 구현 대상으로 유지한다. |
| 비밀번호 재설정 | 후순위로 둔다. | 추후 화면 설계 시 적용 대상과 사용자 흐름을 정한다. |
| 이메일 변경 | 후순위로 둔다. | 추후 화면 설계 시 적용 대상과 사용자 흐름을 정한다. |
| 네이버/구글 OAuth | 진행한다. | 다음 세션에서 카카오 패턴을 기준으로 provider별 실제 연동을 확장한다. |
| 계정 통합 UX | 추가 구체화가 필요하다. | 요청/대상 계정 화면, 승인/취소/만료 안내, 민감정보 마스킹, 알림 정책을 정리한다. |
| 알림 | 샘플 이벤트와 실제 이메일 발송까지 구현한다. | SMTP 환경변수와 발송 실패 기록을 포함한다. |
| DB | dev와 동일하게 우분투 PostgreSQL 기준으로 둔다. | stg/prd도 우분투 PostgreSQL 분리 구성을 우선 적용한다. |
| QNAP NAS 스크립트 | 세션 후반에 진행한다. | HTTPS/공유기 설정과 함께 마무리 작업으로 분리한다. |

## 이번 세션 진행

| 작업 | 상태 | 산출물 |
| :--- | :---: | :--- |
| 계정 통합 알림 이벤트 모델 | 진행 | `src/review/review-notifications.ts` |
| SMTP 실발송 어댑터 | 진행 | `src/review/smtp-email-sender.ts` |
| 검수 서버 알림 연동 | 진행 | `src/review/mvp1-review-server.ts` |
| 검수화면 알림 표시 | 진행 | `src/review/public/*` |
| NAS SMTP 환경변수 예시 | 진행 | `deploy/nas/.env.example`, `deploy/nas/docker-compose.dev.yml` |
| 계정 통합 UX/알림 계획 | 진행 | `docs/superpowers/plans/2026-06-12-account-merge-notification-ux.md` |

## 계정 통합 UX 보완 기준

1. 요청 계정이 다른 계정에 연결된 OAuth 식별자를 연결하려 하면 자동 통합하지 않는다.
2. MVP1 대표 계정은 요청 계정으로 고정한다.
3. 대상 계정은 승인 전 다음 내용을 확인해야 한다.
   - 이전될 로그인 제공자
   - 이전 후 대상 계정이 `merged` 상태가 된다는 점
   - 되돌리기 어려운 작업이라는 점
4. 화면에서는 provider user id를 전체 노출하지 않고 일부 마스킹한다.
5. 승인, 취소, 만료, 중복 승인 실패는 모두 사용자에게 상태 메시지로 표시한다.
6. 요청 생성 시 대상 계정에 알림을 보내고, 승인 완료 시 요청/대상 양쪽에 알림을 보낸다.

## 후속 작업

| 우선순위 | 작업 | 완료 조건 |
| :---: | :--- | :--- |
| 1 | 이메일 인증 실제 API/화면 흐름 구체화 | 인증 요청, 인증 완료, 미인증 접근 제한 기준 확정 |
| 1 | 계정 통합 승인 화면 UX 구현 | 대상 계정 로그인 후 승인/취소/만료 상태를 화면에서 확인 |
| 1 | SMTP 실제 계정으로 발송 검증 | `SMTP_*` 설정 후 요청/승인 알림 수신 확인 |
| 2 | 네이버 OAuth 실제 연동 | 네이버 start/callback, profile normalize, 테스트 추가 |
| 2 | 구글 OAuth 실제 연동 | 구글 start/callback, profile normalize, 테스트 추가 |
| 2 | dev HTTPS 연결 | `https://fowersdev.pdfrend.com` 접속과 카카오 callback 성공 |
| 3 | QNAP NAS 배포 스크립트 | compose pull/up/ps 자동화와 healthcheck 확인 |

## 작업 이력

| 작업일시 | 작업 에이전트 | 작성자 | 내용 한 줄 요약 |
| :--- | :--- | :--- | :--- |
| 2026-06-12 13:20 KST | Codex | jkoogi | MVP1 잔여작업 결정사항과 계정통합 알림 진행 기준 정리 |
