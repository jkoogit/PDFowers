# 260617_02 세션 리뷰 - MVP1 마무리 검증 및 인증 UX

## 1. 작업 개요

| 항목 | 내용 |
| :--- | :--- |
| 작업일시 | 2026-06-17 KST |
| 작업 에이전트 | Codex |
| 작업 브랜치 | `task/카카오콜백환경분리_codex` |
| 작업 목표 | MVP1 마무리 검증 1순위와 인증 UX 보완 2순위 작업을 진행하고, 실제 운영 검증과 제공자 확장은 다음 세션 범위로 분리 |

## 2. 주요 변경

1. Kakao Redirect URI 환경 분리
   - `APP_ENV=dev`에서는 `KAKAO_ALLOWED_REDIRECT_ORIGINS`에 등록된 요청 Host 기준으로 callback URI를 생성한다.
   - `APP_ENV=stg`, `APP_ENV=prd`에서는 기존처럼 `APP_BASE_URL + KAKAO_REDIRECT_PATH` 기준 고정 callback URI를 사용한다.
   - 허용되지 않은 Host는 request-host callback 대상으로 사용하지 않는다.

2. dev NAS IP 직접 접속 검증 보완
   - `http://192.168.219.112:4173`에서 Kakao 로그인을 시작하면 callback도 같은 origin인 `http://192.168.219.112:4173/auth/kakao/callback`을 사용하도록 했다.
   - NAS 컨테이너 내부 healthcheck용 `http://127.0.0.1:4173`도 허용 origin 예시에 포함했다.

3. Kakao callback 인증 UX 보완
   - callback 성공 시 JSON 응답 대신 검수화면 `/`으로 복귀한다.
   - callback 실패와 state mismatch도 오류 코드와 함께 검수화면으로 복귀한다.
   - 검수화면은 query string의 Kakao 결과를 읽어 상태 메시지를 표시하고, 새로고침 중복 표시를 막기 위해 query를 정리한다.

4. 문서 갱신
   - MVP1 잔여작업 문서에 이번 세션 완료 범위와 다음 세션 범위를 분리했다.
   - Kakao 검수 설정, 배포 운영 가이드, NAS 내부망 심화학습 문서에 `APP_ENV=dev` request-host 기준을 반영했다.

## 3. 변경 파일

| 파일 | 내용 |
| :--- | :--- |
| `src/review/kakao-config.ts` | `APP_ENV` 기반 Kakao redirect policy 생성 |
| `src/review/mvp1-review-server.ts` | request-host callback 처리, state별 OAuth client 보관, callback 후 검수화면 redirect |
| `src/review/mvp1-review-runner.ts` | env 기반 redirect policy 연결 |
| `src/review/public/mvp1-review.js` | Kakao callback 결과 메시지 표시 및 query 정리 |
| `test/unit/kakao-config.test.ts` | dev/prd redirect policy 테스트 |
| `test/unit/mvp1-review-kakao-server.test.ts` | request-host callback과 callback redirect UX 테스트 |
| `deploy/nas/.env.example` | `KAKAO_ALLOWED_REDIRECT_ORIGINS` 예시 추가 |
| `deploy/nas/docker-compose.dev.yml` | 허용 origin 환경변수 주입 |
| `deploy/nas/qnap-container-station.dev.yml` | QNAP compose 허용 origin 환경변수 주입 |
| `docs/08_이슈관리/20260612_MVP1_잔여작업_결정및진행.md` | 잔여작업 우선순위와 다음 세션 범위 갱신 |
| `docs/04_기술관리/10_카카오간편로그인_검수설정.md` | dev request-host callback 기준 추가 |
| `docs/04_기술관리/11_배포운영가이드.md` | APP_ENV별 redirect 운영 기준 추가 |
| `docs/09_심화학습/07_NAS_내부망_도메인과_Kakao_로그인.md` | NAS 내부망 IP 직접 접속 검증 기준 보완 |

## 4. 검증 내용

| 검증 | 결과 |
| :--- | :--- |
| `npm run test:unit -- test/unit/mvp1-review-kakao-server.test.ts` | 통과 |
| `npm run typecheck` | 통과 |
| `npm run test:unit` | 통과, 11 files / 63 tests |
| `npm run build` | 통과 |
| `git diff --check` | 통과, 줄바꿈 변환 경고만 확인 |

## 5. 후속 작업

1. 이번 브랜치를 `dev` 대상 PR로 반영한다.
2. GitHub Actions dev 이미지 재빌드 후 NAS Container Station에서 pull/recreate한다.
3. Kakao Developers REST API Redirect URI에 dev 허용 callback URI를 등록한다.
4. dev NAS에서 도메인 접속과 IP 직접 접속 Kakao 로그인 플로우를 수동 검증한다.
5. 다음 세션에서는 실제 운영 검증과 Naver/Google 제공자 확장을 진행한다.

## 6. MVP1 작업현황

### 완료 또는 PR 반영 대기

| 영역 | 상태 | 근거 |
| :--- | :---: | :--- |
| 인증 도메인 코어 | 완료 | `MVP1-AUTH-T001~T020` 단위 테스트 커버리지 확보 |
| PostgreSQL 저장소 | 완료 | Drizzle schema/migration, 저장소 통합 테스트, 기존 인증 상태 hydration 반영 |
| MVP1 검수화면 | 완료 | 검수 서버, 정적 화면, API 시나리오, 상태 패널 제공 |
| Kakao 실제 OAuth | 완료 | start/callback, token/profile 조회, timeout, smoke check 반영 |
| dev NAS 배포 구성 | 완료 | GHCR 이미지 pull 방식 compose, 환경변수 예시, healthcheck 반영 |
| Kakao callback 환경 분리 | PR 반영 대기 | PR #59, `APP_ENV=dev` request-host / `stg/prd` fixed 정책 |
| Kakao callback 인증 UX | PR 반영 대기 | PR #59, callback 후 검수화면 복귀와 결과 메시지 표시 |

### 검증 대기

| 항목 | 필요 조치 | 완료 기준 |
| :--- | :--- | :--- |
| dev 이미지 재빌드 | PR #59 병합 후 GitHub Actions 확인 | `ghcr.io/jkoogit/pdfowers:dev` 최신 이미지 발행 |
| NAS 컨테이너 재기동 | Container Station에서 pull/recreate | `pdfowers_dev` 최신 이미지 기동, healthcheck 정상 |
| Kakao 도메인 로그인 | `https://jkok2.myqnapcloud.com:4443`에서 실제 로그인 | callback 후 검수화면 복귀, 세션 발급, 사용자 상태 반영 |
| Kakao IP 직접 접속 로그인 | `http://192.168.219.112:4173`에서 실제 로그인 | 허용 origin callback으로 state mismatch 없이 처리 |
| Kakao 콘솔 설정 | dev callback URI 등록 | KOE006 없이 authorize/token 교환 성공 |

## 7. 남은 작업 목록

| 우선순위 | 작업 | 범위 | 완료 조건 |
| :---: | :--- | :--- | :--- |
| 1 | PR #59 병합 및 dev 배포 반영 | 현재 세션 산출물 | `task/카카오콜백환경분리_codex -> dev` 병합 |
| 1 | dev NAS 실제 로그인 검증 | Kakao 도메인/IP 접속 | 도메인/IP 접속 모두 callback UX 정상 |
| 1 | 검증 결과 기록 | PR 또는 관련 이슈 댓글 | 이미지 SHA, 컨테이너 재기동, 수동 검증 결과 기록 |
| 2 | 계정 통합 UX 정교화 | 검수화면 메시지/상태 표시 | 승인/취소/만료/중복 승인 상태를 검수자가 화면에서 명확히 확인 |
| 2 | 이메일 인증 UX 구체화 | 검수화면/문서 | 인증 요청, 인증 완료, 미인증 접근 제한 기준 정리 |
| 2 | SMTP 실제 발송 확인 | dev SMTP 설정 | 계정 통합 요청/승인 알림이 실제 계정으로 수신 |
| 3 | stg/prd 실제 운영 검증 | 다음 세션 | Cloudtype, DB, Kakao callback, SMTP 환경 확인 |
| 4 | Naver/Google 제공자 확장 | 다음 세션 | 실제 OAuth start/callback/profile normalize와 테스트 추가 |

## 8. 다음 세션 작업계획 범위

다음 세션은 이번 PR의 dev 반영이 끝났다는 전제에서 시작한다. 범위는 3순위와 4순위로 한정한다.

### 3순위: 실제 운영 검증

1. `dev`, `stg`, `main` 원격 브랜치와 로컬 `main` 현행화 확인
2. PR #59 병합 여부와 dev NAS 배포 상태 확인
3. Cloudtype stg/prd 환경변수 점검
   - `APP_ENV`
   - `APP_BASE_URL`
   - `DATABASE_URL`
   - `KAKAO_REST_API_KEY`
   - `KAKAO_CLIENT_SECRET`
   - `SMTP_*`
4. stg Kakao callback과 DB 연결 smoke 검증
5. prd 승급 전 검증 체크리스트 작성
6. 필요한 경우 `dev -> stg`, `stg -> main` 승급 절차 진행

### 4순위: 제공자 확장

1. Naver OAuth 설정값과 callback URI 정책 확정
2. Google OAuth 설정값과 callback URI 정책 확정
3. Kakao에서 정리한 redirect policy를 제공자 공통 구조로 확장할지 결정
4. Naver start/callback/profile normalize 구현
5. Google start/callback/profile normalize 구현
6. 제공자별 timeout, 실패 응답, 민감정보 비노출 테스트 추가

### 다음 세션 제외 범위

- PDF 업로드, 변환, 주석, 공유 기능
- 운영 관리자 콘솔
- RabbitMQ 등 외부 MQ 도입
- 모바일 앱 전용 인증 플로우
