# 260610_04 작업 리뷰 - DB 통합검증 및 공통코드 보완

## 1. 세션 고유정보

| 항목 | 내용 |
| :--- | :--- |
| 세션 작업일 | 2026-06-10 |
| 작업 브랜치 | `task/DB통합검증-공통코드보완_codex` |
| 작업 에이전트 | Codex |
| 작성자 | jkoogi |
| 기준 브랜치 | `origin/main` |
| GitHub 이슈 | [#29 DB 통합검증과 공통코드 초기데이터 보완](https://github.com/jkoogit/PDFowers/issues/29) |
| 참고 리뷰 | `docs/10_리뷰관리/202606/10/260610_03_작업리뷰_MVP1테스트분리및DB관리이력.md` |

## 2. 시작 점검

| 점검 항목 | 결과 |
| :--- | :--- |
| 원격 최신화 | `git fetch --all --prune` 완료 |
| 로컬 main | `origin/main` 기준 fast-forward 완료 |
| 원격 브랜치 SHA | `origin/main=e7dbbfd`, `origin/stg=bd28f87`, `origin/dev=e3a2ca2` |
| 브랜치 판단 | 세 브랜치는 동일 SHA가 아니나, 최근 `dev -> stg -> main` 승급 커밋 흐름으로 확인 |

## 3. 정책 판단

| 항목 | 판단 |
| :--- | :--- |
| 시스템 초기데이터 범위 | 공통코드, 시스템 설정, 기본 정책값만 관리한다. 회원정보, 감사 로그 row, Job 실행 row는 활동 데이터로 제외한다. |
| 공통코드 관리 방식 | MVP1 단계에서는 `common_code` 테이블과 seed 기준 파일을 먼저 둔다. 실제 DB upsert 스크립트는 dev DB 적용 검증 시 확정한다. |
| DB URL 형식 | Java/JDBC 문서값은 `jdbc:postgresql://...`를 쓸 수 있으나 Node/Drizzle 실행값은 `postgresql://...`로 정규화한다. |
| 보안 점검 | `npm audit`는 외부 npm registry로 의존성 메타데이터를 전송하므로 명시 승인 전에는 수행하지 않는다. |

## 4. 작업 요약

| 구분 | 내용 |
| :--- | :--- |
| DB 스키마 | `common_code` Drizzle 테이블 추가 |
| Migration | `src/db/migrations/0001_gray_joseph.sql` 생성 |
| Seed 기준 | `src/db/seed/common-codes.ts`에 MVP1 공통코드 27건 작성 |
| Seed 적용 | `0001_gray_joseph.sql`에 공통코드 27건 upsert 포함 |
| DB URL | `DATABASE_URL` 단일 기준 반영 |
| 테스트 | 공통코드 seed 단위 테스트 추가 |
| 통합 테스트 보조 DDL | `ensureAuthSchema`에 `common_code` 생성 포함 |
| 문서 | 시스템 DB 관리이력과 개발 명령어 문서 갱신 |

## 5. 변경 파일

| 파일 | 변경 내용 |
| :--- | :--- |
| `src/db/schema/auth.ts` | `common_code` 테이블과 복합 PK 정의 |
| `src/db/migrations/0001_gray_joseph.sql` | `common_code` 생성 migration |
| `src/db/migrations/meta/*` | Drizzle migration 메타데이터 갱신 |
| `src/db/seed/common-codes.ts` | `DBD-20260610-001`~`027` 공통코드 seed 기준 작성 |
| `src/db/database-url.ts` | JDBC URL 정규화와 환경별 DB URL 선택 함수 추가 |
| `drizzle.config.ts` | `DATABASE_URL` 단일 기준 반영 |
| `.env.example` | 삭제. 로컬은 Git 제외 `.env`, 배포는 배포 시스템 환경변수 주입 기준으로 정리 |
| `test/unit/common-code-seed.test.ts` | seed 건수, 주요 코드값, 중복 방지, 그룹별 정렬 검증 |
| `test/unit/database-url.test.ts` | JDBC URL 정규화와 환경별 DB URL 선택 검증 |
| `src/domains/auth/auth-repository.ts` | 통합 테스트 보조 schema 생성에 `common_code` 포함 |
| `docs/03_설계관리/데이터관리/02_시스템DB관리이력.md` | `common_code` 이력과 seed 전환 상태 반영 |
| `docs/04_기술관리/06_개발명령어.md` | `db:generate`와 seed 기준 파일 위치 반영 |

## 6. 검증 결과

| 명령 | 결과 |
| :--- | :--- |
| `npm run test:unit -- test/unit/common-code-seed.test.ts` | 통과, 2개 파일 24개 테스트 성공 |
| `npm test` | 통과, 30개 성공 |
| `npm run typecheck` | 통과 |
| `npm run db:generate` | 재실행 시 schema 변경 없음 |
| `npm run db:migrate` | 통과, dev DB migration 적용 |
| `npm run test:integration` | 통과, 3개 성공 |
| `git diff --check` | 통과, CRLF 변환 경고만 표시 |
| `npm audit --audit-level=moderate` | 외부 registry 전송 위험으로 자동 승인 거절 |

## 7. 남은 작업

| 우선순위 | 작업 | 완료 조건 |
| :---: | :--- | :--- |
| 완료 | dev DB migration 적용 | `pdfowers_dev`에 `0000`, `0001` migration 적용 완료 |
| 완료 | 실제 DB 통합 테스트 실행 | `DATABASE_URL` 기준 `npm run test:integration` 3개 통과 |
| 완료 | 공통코드 seed 적용 검증 | `0001` migration 기준 `common_code` 27건과 `AUTH_PROVIDER` 3건 통합 테스트 검증 |
| 2 | 보안 audit 승인 후 점검 | 사용자가 외부 npm registry 전송을 승인하면 `npm audit` 결과 검토 |
| 2 | 시스템 설정 테이블 검토 | 이메일 인증 필수 여부 등 환경별 기본 정책값 관리 방식 확정 |

## 8. 작업 이력

| 작업일시 | 작업 에이전트 | 작성자 | 내용 한 줄 요약 |
| :--- | :--- | :--- | :--- |
| 2026-06-10 23:20 KST | Codex | jkoogi | 공통코드 테이블과 MVP1 공통코드 seed 기준 작성, DB 적용 검증 후속 작업 정리 |
| 2026-06-10 23:25 KST | Codex | jkoogi | `DATABASE_URL` 단일 기준과 공통코드 migration upsert 반영 |
| 2026-06-11 00:05 KST | Codex | jkoogi | `.env.example` 삭제, dev DB migration/통합 테스트/공통코드 seed 검증 완료 |
